import { useId } from 'react'; import type { TextareaHTMLAttributes } from 'react'; import type { FieldSize } from '../TextField/TextField'; import '../FormField/FormField.css';
type Props = { label?: string; helperText?: string; error?: string; size?: FieldSize; fullWidth?: boolean } & TextareaHTMLAttributes<HTMLTextAreaElement>;
export function Textarea({ label, helperText, error, size = 'md', fullWidth = false, id, required, className = '', ...props }: Props) { const generatedId = useId(); const fieldId = id ?? generatedId; return <div className={`ui-field ui-field--${size} ${fullWidth ? 'ui-field--full' : ''} ${error ? 'ui-field--error' : ''} ${className}`.trim()}>{label && <label className="ui-field__label" htmlFor={fieldId}>{label}{required && <span className="ui-field__required" aria-hidden="true">*</span>}</label>}<textarea id={fieldId} className="ui-field__textarea" required={required} {...props} />{(error || helperText) && <p className={`ui-field__hint ${error ? 'ui-field__hint--error' : ''}`} role={error ? 'alert' : undefined}>{error ?? helperText}</p>}</div>; }
