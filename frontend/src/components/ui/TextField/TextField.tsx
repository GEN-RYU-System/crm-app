import { useId } from 'react'; import type { InputHTMLAttributes } from 'react'; import '../FormField/FormField.css';
export type FieldSize = 'sm' | 'md' | 'lg'; type Props = { label?: string; helperText?: string; error?: string; size?: FieldSize; fullWidth?: boolean } & Omit<InputHTMLAttributes<HTMLInputElement>, 'size'>;
export function TextField({ label, helperText, error, size = 'md', fullWidth = false, id, required, className = '', ...props }: Props) { const generatedId = useId(); const fieldId = id ?? generatedId; return <div className={`ui-field ui-field--${size} ${fullWidth ? 'ui-field--full' : ''} ${error ? 'ui-field--error' : ''} ${className}`.trim()}>{label && <label className="ui-field__label" htmlFor={fieldId}>{label}{required && <span className="ui-field__required" aria-hidden="true">*</span>}</label>}<input id={fieldId} className="ui-field__input" required={required} {...props} />{(error || helperText) && <p className={`ui-field__hint ${error ? 'ui-field__hint--error' : ''}`} role={error ? 'alert' : undefined}>{error ?? helperText}</p>}</div>; }
