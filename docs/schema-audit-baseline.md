# Core Schema V1 適合性監査 ベースライン

> 作成日: 2026-08-30  
> 目的: 「既存差異」の内訳を内訳ごとに記録し、今後の比較基準とする  
> 背景: PR #680 後の監査で総不一致6件を記録したが、「修正前から存在する」と断言できる  
> 　　　過去ログの記録（総不一致2件・内訳数値なし）と照合できなかった。  
> 　　　以降は本ファイルの数値と比較することで「増えたか / 同一か」を判定できるようにする。

---

## 1. 取得情報

| 項目 | 値 |
|-----|-----|
| 取得日時（JST） | 2026-08-30 |
| `getDeployedSha` | `d4d69cfa4fbdfd2cfb4c98035dec0cc0b0aaeb1c` |
| `deployedAt` | `2026-08-30T11:50:47.212Z` |
| `origin/develop` HEAD（監査時点） | `75c7c7276c7a43882295353acb277474920dd2d2` |
| HEAD commit | `docs: PR #681 作業ログ追記（発送タブ フォーム・全項目表示） (#682)` |
| 備考 | `75c7c72` は docs のみの変更（GAS ソース変更なし）。GAS としては `d4d69cf`（PR #681）が最新デプロイ。 |

---

## 2. 監査の生出力（全文・要約なし）

```
=== Core Schema V1 適合性監査 ===

[LEADS / リード管理]
  1. シート取得: OK (リード管理)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 51 / 実シート 64 → ★不一致 (差: 13)
  4. 主キー列 (リードID): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 1件

[CUSTOMERS / 顧客マスタ]
  1. シート取得: OK (顧客マスタ)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 14 / 実シート 15 → ★不一致 (差: 1)
  4. 主キー列 (顧客ID): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 1件

[SHIPPING_DESTINATIONS / 配送先マスタ]
  1. シート取得: OK (配送先マスタ)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 17 / 実シート 17 → OK
  4. 主キー列 (配送先ID): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

[PAYMENT_DESTINATIONS / 支払先マスタ]
  1. シート取得: OK (支払先マスタ)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 16 / 実シート 16 → OK
  4. 主キー列 (支払先ID): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

[ORDERS / オーダー管理]
  1. シート取得: OK (オーダー管理)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 43 / 実シート 43 → OK
  4. 主キー列 (オーダーID): OK
  5. Values [PAYMENT_STATUS] (支払いステータス): OK
  5. Values [PAYMENT_CONFIRMATION_SOURCE] (入金確認元): OK
  5. Values [PAYMENT_METHOD] (決済手段): OK
  5. Values [STATUS] (ステータス): OK
  小計不一致: 0件

[ORDER_LINES / オーダー明細]
  1. シート取得: OK (オーダー明細)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 11 / 実シート 11 → OK
  4. 主キー列 (明細ID): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

[QUOTES / 見積もり管理]
  1. シート取得: OK (見積もり管理)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 19 / 実シート 19 → OK
  4. 主キー列 (見積書ID): OK
  5. Values [STATUS] (ステータス): OK
  小計不一致: 0件

[ISSUER / 発行元マスタ]
  1. シート取得: OK (発行元マスタ)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 18 / 実シート 18 → OK
  4. 主キー列 (発行元ID): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

[QUOTE_LINES / 見積もり明細]
  1. シート取得: OK (見積もり明細)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 12 / 実シート 12 → OK
  4. 主キー列 (明細ID): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

[SHIPMENTS / 発送]
  1. シート取得: OK (発送)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 20 / 実シート 20 → OK
  4. 主キー列 (発送ID): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

[PURCHASES / 仕入れ]
  1. シート取得: OK (仕入れ)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 19 / 実シート 19 → OK
  4. 主キー列 (仕入れID): OK
  5. Values [STATUS] (ステータス): OK
  小計不一致: 0件

[FORM_TOKENS / フォームトークン]
  1. シート取得: OK (フォームトークン)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 4 / 実シート 4 → OK
  4. 主キー列 (トークン): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

[PRODUCTS / 商品マスタ同期]
  1. シート取得: OK (商品マスタ同期)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 24 / 実シート 24 → OK
  4. 主キー列 (product_id): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

[STAFF / 担当者マスタ]
  1. シート取得: OK (担当者マスタ)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 24 / 実シート 24 → OK
  4. 主キー列 (担当者ID): OK
  5. Values [ROLE] (役割): OK
  5. Values [STATUS] (ステータス): OK
  小計不一致: 0件

[LOGIN_SESSIONS / ログインセッション]
  1. シート取得: OK (ログインセッション)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 6 / 実シート 6 → OK
  4. 主キー列 (セッションID): OK
  5. Values [STATUS] (状態): OK
  小計不一致: 0件

[SHARED_INVENTORY / 共用在庫]
  1. シート取得: OK (共用在庫)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 11 / 実シート 11 → OK
  4. 主キー: null (スキップ)
  5. Values [CONDITION] (Condition): ★実データに未定義値 4種 → Searched pack, FLAG_SINGLE, Damaged case, Unsearched pack
  小計不一致: 4件

[CURRENCIES / 通貨マスタ]
  1. シート取得: OK (通貨マスタ)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 5 / 実シート 5 → OK
  4. 主キー列 (通貨コード): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

[LEAD_SOURCES / 流入元マスタ]
  1. シート取得: OK (流入元マスタ)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 6 / 実シート 6 → OK
  4. 主キー列 (source_id): OK
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

[SETTINGS / システム設定]
  1. シート取得: OK (システム設定)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 5 / 実シート 5 → OK
  4. 主キー列 (設定キー): OK
  5. Values [VALUE_TYPE] (値の型): OK
  小計不一致: 0件

[LEGACY_INPUT / 請求書作成]
  1. シート取得: OK (請求書作成)
  2-3. headers: {} → ヘッダー/列数チェックをスキップ
  小計不一致: 0件

[LEGACY_SALES / 📊売上データ]
  1. シート取得: OK (📊売上データ)
  2-3. headers: {} → ヘッダー/列数チェックをスキップ
  小計不一致: 0件

[DISPLAY_SETTINGS / 表示設定マスタ]
  1. シート取得: OK (表示設定マスタ)
  2. 定義→実シート 欠落ヘッダー: なし
  3. ヘッダー列数: 定義 4 / 実シート 4 → OK
  4. 主キー: null (スキップ)
  5. Values: 定義なし (スキップ)
  小計不一致: 0件

=== 総不一致: 6 → ★FAIL ===
```

---

## 3. 内訳表

| テーブル | 不一致種別 | 定義列数 | 実シート列数 | 差 | 具体内容 |
|---------|-----------|---------|------------|-----|---------|
| LEADS / リード管理 | ヘッダー列数 | 51 | 64 | +13 | 下記 3-1 参照 |
| CUSTOMERS / 顧客マスタ | ヘッダー列数 | 14 | 15 | +1 | 下記 3-2 参照 |
| SHARED_INVENTORY / 共用在庫 | CONDITION 値 | — | — | 4種 | 下記 3-3 参照 |
| **合計** | | | | | **6件** |
| ORDERS / オーダー管理 | — | 43 | 43 | 0 | **0件（常時必達）** |

### 3-1. LEADS 差13列 — 実シートにあってコード定義にない列

出典: `docs/sheet-headers-snapshot.md`（PR #674 でスプレッドシートから実測）  
照合方法: 実シート64列ヘッダーリスト − コード定義51列を差分

| # | 実シートの列ヘッダー名 | コード定義（src/00_CoreSchemaRegistry.js）での有無 |
|---|-------------------|--------------------------------------------|
| 1 | `リード進捗` | なし |
| 2 | `商談進捗` | なし |
| 3 | `1回の発注金額` | なし |
| 4 | `購入頻度(月次)` | なし |
| 5 | `商談の手応え` | なし |
| 6 | `Good Point` | なし |
| 7 | `More Point` | なし |
| 8 | `反省と今後の抱負` | なし |
| 9 | `レポート提出日` | なし |
| 10 | `レポート確認者` | なし |
| 11 | `レポート確認日` | なし |
| 12 | `レポートコメント` | なし |
| 13 | `Buddyフィードバック` | なし |

監査の「定義→実シート 欠落ヘッダー: なし」から、コード定義の51列はすべて実シートに存在する。  
よって差分13列は**「実シートにあってコード定義にない」（定義漏れ）**。

### 3-2. CUSTOMERS 差1列 — 実シートにあってコード定義にない列

出典: `docs/sheet-headers-snapshot.md`（顧客マスタ 15列）  
実シートヘッダー全15列:  
`顧客ID / 源流リードID / 顧客名 / 国 / メール / 電話番号 / 国番号 / 初回取引日 / 登録日 / 営業担当者 / 担当者ID / 連絡ツール / FedEx ID / 発送時メモ / 顧客規模`

コード定義14列（src/00_CoreSchemaRegistry.js:16）:  
`顧客ID / 源流リードID / 顧客名 / 国 / メール / 電話番号 / 国番号 / 初回取引日 / 登録日 / 営業担当者 / 連絡ツール / FedEx ID / 発送時メモ / 顧客規模`

差分: **`担当者ID`**（実シートの11列目に存在。コード定義にない）

参考: `AUTONOMOUS_WORK_LOG.md:274` に「`担当者ID` は PR #590 対応、別セッション管轄」の記述あり。

### 3-3. SHARED_INVENTORY CONDITION 未定義値4種

コード定義値（src/00_CoreSchemaRegistry.js:205-210）:
```javascript
CONDITION: {
  SEALED_BOX:         'Sealed box',
  DAMAGED_SEALED_BOX: 'Damaged sealed box',
  CASE:               'Case',
  NO_SHRINK_BOX:      'No shrink box'
}
```

実シートに存在するが定義にない値:
| 未定義値 | 実シートの列 | 行数 |
|---------|-----------|-----|
| `Searched pack` | Condition 列（4列目） | 【未確認】 |
| `FLAG_SINGLE` | Condition 列（4列目） | 【未確認】 |
| `Damaged case` | Condition 列（4列目） | 【未確認】 |
| `Unsearched pack` | Condition 列（4列目） | 【未確認】 |

備考: Condition列の位置は `src/00_CoreSchemaRegistry.js:200` の定義順（4番目の列）から特定。  
各値の行数は `clasp run` の読み取り専用関数から取得可能だが、本ベースライン作成時点では実行していない。「何行分存在するか」は【未確認】。

---

## 4. 差異の性質（調査のみ・修正の判断はしない）

定義: 「実シートにあってコード定義にない」= **定義漏れ** / 「コード定義にあって実シートにない」= **定義過剰**

| テーブル | 判定 | 根拠 |
|---------|------|------|
| LEADS 差13列 | **定義漏れ**（実シートにあり・コード定義になし） | 監査「欠落ヘッダー: なし」= 定義列はすべて実シートに存在。差分13列はすべて実シート側にのみ存在（src/00_CoreSchemaRegistry.js:4-5 に記述なし） |
| CUSTOMERS 差1列（担当者ID） | **定義漏れ**（実シートにあり・コード定義になし） | 同上。実シート11列目に `担当者ID` が存在するが src/00_CoreSchemaRegistry.js:16 の CUSTOMERS 定義に含まれていない |
| SHARED_INVENTORY 未定義値4種 | **定義漏れ**（実シートにあり・コード定義になし） | 実シートに `Searched pack` / `FLAG_SINGLE` / `Damaged case` / `Unsearched pack` が存在するが src/00_CoreSchemaRegistry.js:205-210 の CONDITION 定義に含まれていない |

**注意: どちらが正しいかはここでは判定しない。** 実シートとコードのどちらを正とするかは PO の業務判断による。

---

## 5. 【未確認】項目

| 項目 | 未確認の内容 | 確認方法 |
|-----|-----------|---------|
| SHARED_INVENTORY 未定義値 各行数 | `Searched pack` / `FLAG_SINGLE` / `Damaged case` / `Unsearched pack` が実シートに何行分存在するか | 読み取り専用の `clasp run` 関数で取得可能 |
| LEADS 差13列の追加経緯 | いつ・どのPRで実シートに追加されたか（コード定義への追加漏れか、意図的な未登録か） | git log / PR 一覧で追跡可能 |
| CUSTOMERS 担当者ID の追加経緯 | AUTONOMOUS_WORK_LOG:274 に「PR #590 対応」の記述あるが、PR #590 の差分確認はしていない | `gh pr view 590` |

---

## 6. PO 判断が必要な項目

| # | 項目 | 内容 |
|---|-----|------|
| 1 | SHARED_INVENTORY CONDITION 未定義値4種 | `Searched pack` / `FLAG_SINGLE` / `Damaged case` / `Unsearched pack` は業務上正しい値か。正しい場合はコード定義（src/00_CoreSchemaRegistry.js:205-210）への追加が必要 |
| 2 | LEADS 差13列 | `リード進捗` / `商談進捗` / `1回の発注金額` 等13列をコード定義に追加するか、実シートから削除するか |
| 3 | CUSTOMERS 担当者ID | 実シートの `担当者ID` 列をコード定義に追加するか |

---

## 7. 今後の判定手順

**次回以降の `runCoreSchemaConformanceAudit` 実行時:**

1. 本ファイルの「内訳表」（セクション3）と数値を比較する
2. 総不一致件数および内訳が**本ファイルと同一** → 通過（`AUTONOMOUS_WORK_RULES.md` 参照）
3. 1件でも増えた、または内訳が変化した → 直前PRを revert して停止・報告

**ベースライン値（比較用・まとめ）:**

```
総不一致: 6件
  LEADS:            差 13列（定義51 / 実64）
  CUSTOMERS:        差 1列（定義14 / 実15、欠落列: 担当者ID）
  SHARED_INVENTORY: 未定義値 4種（Searched pack / FLAG_SINGLE / Damaged case / Unsearched pack）
  ORDERS:           0件（常時必達）
```

このベースラインが変化した場合（PO の指示による修正を含む）は、  
本ファイルのベースライン値も同時に更新すること。
