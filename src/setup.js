/**
 * プロジェクト初期化用。一度だけ実行してください。
 */
function setupProject() {
    initSystemSheets();
    console.log("✅ システム管理用シート（90, 91, 99）を初期化しました。");
    console.log("⚠️ 動作にはスクリプトプロパティ 'DISCORD_CRM_WEBHOOK' の設定が必要です。");
}
